<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base XML theme — Compose aplica o Material3 em runtime -->
    <style name="Theme.Cadernim" parent="Theme.AppCompat.DayNight.NoActionBar" />

    <style name="Theme.Cadernim.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#1B4332</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Cadernim</item>
    </style>

</resources>
